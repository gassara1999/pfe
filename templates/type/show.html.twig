{% extends 'base.html.twig' %}

{% block title %}MembershipType{% endblock %}

{% block body %}
    <h1>MembershipType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membership_type.id }}</td>
            </tr>
            <tr>
                <th>Membershiptype</th>
                <td>{{ membership_type.Membershiptype }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ membership_type.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ membership_type.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_index') }}">back to list</a>

    <a href="{{ path('app_type_edit', {'id': membership_type.id}) }}">edit</a>

    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
