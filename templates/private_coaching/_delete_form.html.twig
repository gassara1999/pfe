<form method="post" action="{{ path('app_private_coaching_delete', {'id': private_coaching.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ private_coaching.id) }}">
    <button type="button" class="btn btn-outline-danger">Delete</button>
</form>
