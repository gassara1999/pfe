{% extends 'base.html.twig' %}

{% block title %}Edit Membership{% endblock %}

{% block body %}
	<div class="edit-Container">
    <h1>Edit Membership</h1>
    <div class="col-12">
            <p align="right">
                <a href="{{ path('app_membership_new') }}" class="btn btn-outline-success">back to list</a>
            </p>
        </div>

    {{ include('membership/_form.html.twig', {'button_label': 'Update'}) }}


    {{ include('membership/_delete_form.html.twig') }}
    </div>
{% endblock %}
